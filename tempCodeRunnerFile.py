= load_listing_results(html_path)
    # database = []
    # for listing_title, listing_id in listings:
    #     details = get_listing_details(listing_id)
    #     info = details[listing_id]
    #     row = (
    #         listing_title,
    #         listing_id,
    #         info["policy_number"],
    #         info["host_type"],
    #         info["host_name"],
    #         info["room_type"],
    #         info["location_rating"]
    #     )
    #     database.append(row)
    # return database